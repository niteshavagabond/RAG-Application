npm create vite@latest my-simple-homepage -- --template vanilla-ts  #incase you want to create a new folder

npm install


# to create virtual environment
python -m venv .venv

# to run the frontend
npm run dev 

# to run the backend
uvicorn main:app --reload